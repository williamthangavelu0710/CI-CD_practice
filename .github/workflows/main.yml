name: Hello World

on:
  push:
    paths-ignore:
      - '.github/**'
  workflow_dispatch:

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo "Hello, World!"
